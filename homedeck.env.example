# HomeDeck Environment Variables for Systemd
# Copy this file to /etc/homedeck/homedeck.env and configure your values
# This file should be readable only by root (chmod 600)

# Home Assistant Configuration
# Use "wss" for secure websocket, "ws" for normal websocket
HA_HOST=ws://192.168.1.100:8123

# Long-lived access token from Home Assistant
# Get it from: Profile -> Long-Lived Access Tokens -> Create Token
HA_ACCESS_TOKEN=your_token_here

# Timezone (optional)
# Check "TZ identifier" column: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TIMEZONE=Europe/Lisbon

# mDNS service identifier (optional)
# Must be unique within the local network
# Accepted characters: A-Z, a-z, 0-9, - _ and spaces
# Leave empty to use IP address
MDNS_SERVICE_ID=

# Icon cache (optional, default: 1)
# Set to 0 to disable icon caching
ENABLE_CACHE=1

# Image optimization (optional, default: 0 for better performance)
# Set to 1 to enable optipng optimization (slower but smaller files)
ENABLE_OPTIPNG=0
